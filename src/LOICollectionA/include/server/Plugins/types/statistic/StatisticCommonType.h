#pragma once

namespace LOICollection::server::Plugins {
    enum class StatisticType {
        onlinetime,
        kills,
        deaths,
        place,
        destroy,
        respawn,
        join
    };
}

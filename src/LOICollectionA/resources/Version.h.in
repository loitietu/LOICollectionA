#pragma once

#define VERSION_MAJOR ${VERSION_MAJOR}
#define VERSION_MINOR ${VERSION_MINOR}
#define VERSION_PATCH ${VERSION_PATCH}
#define VERSION_BUILD ${VERSION_BUILD}

#define STRING(x) #x 

#define COMPANY_NAME STRING(${COMPANY_NAME})

#define FILE_VERSION_FLAG 0x00000000L
#define FILE_DESCRIPTION STRING(${FILE_DESCRIPTION})

#define VERSION_STRING STRING(VERSION_MAJOR) "." STRING(VERSION_MINOR) "." STRING(VERSION_PATCH) "." STRING(VERSION_BUILD)
